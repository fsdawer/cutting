global:
  scrape_interval: 10s # 10초마다 메트릭 수집

scrape_configs:
  - job_name: 'spring-boot-beauty-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080']
